apiVersion: exec.lovable.dev/v1
kind: LifecycleScript
metadata:
  name: 99-lint
spec:
  purpose: lint
  command: "bash -c 'export PATH=$HOME/.cargo/bin:$PATH && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all --check'"
