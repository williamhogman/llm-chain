apiVersion: exec.lovable.dev/v1
kind: LifecycleScript
metadata:
  name: 99-lint
spec:
  purpose: lint
  command: "bash -c 'source scripts/rust-env.sh && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all --check'"
